Mycp
flags:
1. -f
2. -v
3. -i
It must works with full names
Tip: getopt, getopt_long
-----------------------------
./mycp path/file1(must exist) path/file2(if it exists rewrite, if doesn't exist - create and write)
./mycp path/file1 path/file2 ... path/fileN directory(path)